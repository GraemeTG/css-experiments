{
  "name": " ",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "watch": {
    "build": {
      "patterns": [
        "/",
        "javascript-30/"
      ],
      "extensions": "js html css"
    }
  },
  
  "scripts": {
    "build": "parcel build index.html 1-box-*.html *.html javascript-30/*.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm-watch"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "npm-watch": "^0.11.0"
  }
}
